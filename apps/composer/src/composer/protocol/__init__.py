"""Generated pydantic models for the WisprTest protocol.

The models live in `composer.protocol.models`, generated from
`packages/protocol/dist/schema.json` by `pnpm --filter protocol gen:python`. Import from that
module directly:

    from composer.protocol.models import CompositionPlan, ConstraintSet

This package deliberately re-exports nothing. A hand-maintained list of names here would be one
more thing to keep in step with the contract, which is the exact drift the generator exists to
prevent.
"""
