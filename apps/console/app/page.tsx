export default function Home() {
  return (
    <main>
      <h1>WisprTest console</h1>
      <p>
        Scaffold only. The console screens — connect, indexing, product memory, data, sessions,
        drift and admin — are delivered by Phase 18 of the build plan, once the services they read
        from exist.
      </p>
    </main>
  );
}
