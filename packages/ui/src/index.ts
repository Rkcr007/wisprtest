/**
 * `ui` — the shared design system.
 *
 * Semantic design tokens, `useDraggable`, and the HUD primitives (Reticle, GhostCursor,
 * VadBars, Chip, Cell, Toast). Consumed by both the extension HUD and the console so the two
 * surfaces cannot drift visually.
 *
 * Delivered by Phase 6 of docs/BUILD-PLAN.md. Empty by design until then.
 */

export {};
