/**
 * `protocol` — the contract.
 *
 * Zod schemas for every payload that crosses a process boundary, with TypeScript types
 * derived via `z.infer` and pydantic v2 models generated from the same JSON Schema export.
 * Nothing else in the repository defines these shapes.
 *
 * Delivered by Phase 1 of docs/BUILD-PLAN.md. Empty by design until then: Phase 0 is the
 * scaffold, and inventing shapes here ahead of the contract phase would be the exact drift
 * this package exists to prevent.
 */

export {};
