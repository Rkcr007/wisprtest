node_modules/
dist/
coverage/
apps/console/.next/
apps/console/next-env.d.ts
pnpm-lock.yaml
.venv/

# Generated by `pnpm --filter protocol gen:python`. Reformatting it here would make the next
# regeneration a diff, which is exactly the drift the generator exists to prevent.
apps/composer/tests/protocol_fixtures.json

# The specification documents are hand-maintained: their line breaks, table alignment and
# ASCII diagrams are deliberate and Prettier reflows them into something harder to read.
CLAUDE.md
docs/
